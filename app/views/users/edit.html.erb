<h1>Editing user</h1>

<%= error_messages_for :user %>

<%= render :partial => @user, :locals => { :button_name => 'Update' } %>

<%= link_to 'Show', [:admin,@user] %> |
<%= link_to 'Back', admin_users_path %>
